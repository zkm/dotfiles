# Imported modular layout (ML4W-style)
# Right monitor: vertical (landscape)
monitor=DP-3,preferred,0x-1600,1,transform,1
# Left monitor: horizontal (portrait)
monitor=DP-2,preferred,2160x0,1

# Fallback to any connected display if connector names change.
monitor=,preferred,auto,1
