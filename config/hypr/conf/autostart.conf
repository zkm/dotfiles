# Autostart
exec-once=sh -c 'command -v waybar >/dev/null 2>&1 && waybar'
exec-once=sh -c 'command -v hyprpaper >/dev/null 2>&1 && hyprpaper'
exec-once=kitty
exec-once=sh -c '[ -n "${HYPRLAND_INSTANCE_SIGNATURE:-}" ] && command -v mako >/dev/null 2>&1 && mako'
exec-once=nm-applet
