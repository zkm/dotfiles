"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const wasm_1 = require("../wasm");
const util_1 = require("./util");
function lint(text, onError) {
    console.log('running lint() from prisma-fmt');
    try {
        if (process.env.FORCE_PANIC_PRISMA_FMT) {
            (0, util_1.handleFormatPanic)(() => {
                console.debug('Triggering a Rust panic...');
                wasm_1.prismaFmt.debug_panic();
            });
        }
        const result = wasm_1.prismaFmt.lint(text);
        return JSON.parse(result);
    }
    catch (e) {
        const err = e;
        (0, util_1.handleWasmError)(err, 'lint', onError);
        return [];
    }
}
exports.default = lint;
//# sourceMappingURL=lint.js.map