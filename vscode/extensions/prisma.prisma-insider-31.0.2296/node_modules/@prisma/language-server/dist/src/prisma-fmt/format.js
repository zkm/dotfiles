"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const wasm_1 = require("../wasm");
const util_1 = require("./util");
function format(schema, options, onError) {
    console.log('running format() from prisma-fmt');
    try {
        if (process.env.FORCE_PANIC_PRISMA_FMT) {
            (0, util_1.handleFormatPanic)(() => {
                console.debug('Triggering a Rust panic...');
                wasm_1.prismaFmt.debug_panic();
            });
        }
        return wasm_1.prismaFmt.format(schema, JSON.stringify(options));
    }
    catch (e) {
        const err = e;
        console.warn('\nprisma-fmt errored during formatting. Please report this issue on [Prisma Language Tools](https://github.com/prisma/language-tools/issues). \nLinter output:\n');
        (0, util_1.handleWasmError)(err, 'format', onError);
        return schema;
    }
}
exports.default = format;
//# sourceMappingURL=format.js.map