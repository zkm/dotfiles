"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const wasm_1 = require("../wasm");
const util_1 = require("./util");
function nativeTypeConstructors(text, onError) {
    try {
        if (process.env.FORCE_PANIC_PRISMA_FMT_LOCAL) {
            (0, util_1.handleFormatPanic)(() => {
                console.debug('Triggering a Rust panic...');
                wasm_1.prismaFmt.debug_panic();
            });
        }
        const result = wasm_1.prismaFmt.native_types(text);
        return JSON.parse(result);
    }
    catch (e) {
        const err = e;
        (0, util_1.handleWasmError)(err, 'native_types', onError);
        return [];
    }
}
exports.default = nativeTypeConstructors;
//# sourceMappingURL=nativeTypes.js.map